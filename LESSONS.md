# Promoted lessons (cross-project)

**Copyright (c) 2026 Martial Systems LLC.** MIT.

Grok does **not** remember a fix from one repo in the next chat. This file does.

At the start of implement or UI work: apply every row whose **When** matches, unless **Skip when** matches. If you discover a general miss, add a row here and on the drop-ins. Do not leave it only in the project that hit it.

| Date | Lesson | When | Skip when |
|------|--------|------|-----------|
| 2026-09-03 | GHCND (or similar) official station names on a grouped-bar x-axis overlap (`FT WAYNE INTL AP` into `INDIANAPOLIS`). Use the product's short city labels and rotate the ticks. Assert tick boxes do not overlap after `canvas.draw`. A layout restamp of a locked figure is a new `copy.sha`; the science SHA stays. | Bar or categorical axis labeled with official long station names | Axis already uses short names that fit; one or two ticks |
| 2026-09-03 | A catalog that only links GitHub is not a write-up. When the user asks for gist-level explanation on Pages, ship `trees/<id>/index.html` in the console chrome: question, locked SHA, tables, figures, what holds. Title href goes to that page; slug/SHA stay GitHub. That is a study page, not an outlook hero. | Martialsystems research console / Pages and a named tree write-up | GitHub README is the only public write-up the user asked for |
| 2026-09-02 | A table-row click handler that preventDefault on a data-* also used for filtering (data-lane on both lane buttons and <tr>) will steal inner GitHub <a href>, including clicks on <code class="slug"> inside the <a>. color: inherit with no distinct underline makes those hrefs look like body text, so the user clicks "the link" and gets Maps or nothing. Guard closest("a[href]") first and return without preventDefault. Style #catalog a / #ledger a with underline and a distinct color. Bind filter clicks to the key buttons, not every [data-lane]. | HTML tables with row click plus inner <a href> | No inner links; handler already ignores a[href] and filter attrs are not on the row |
| 2026-08-30 | A rare-class gate sample (SFHA, floodway) is not verified by a namesake river coordinate. Read the live classified raster at that lon/lat and refuse if the cell is the majority class or nodata. Majority-class downtown X is necessary and not sufficient. | Rasterizing a classified map with mixed-class gate samples | Unit-only synthetic grids; the layer is a point inventory |
| 2026-08-29 | A live agency product URL that is reused for the next issuance is not a durable citation. Snapshot the issued file with the issue date (CPC lead-4 GIFs here). The live URL will show next month's product. | Public page that cites a monthly or seasonal official product whose path is overwritten | The agency already has a dated archive URL you use; or text-only with issue date and no map/file |
| 2026-09-03 | Indiana research readable index is the live console (martialsystems.github.io/indiana_wx_pages). Gist 66b896b0 is a pointer. New weather git: pins.d on indiana_research_console, ingest, rebuild Pages, lane gist, README footer as a solid-green shields button (`Open_the_research_console-2e7d32`, no `labelColor`) whose href is the Pages URL. Do not paste a visible index URL. Do not keep a full tree list in the gist or .github. Home rule: ~/.grok/rules/weather-research.md | Creating or first-publishing a martialsystems weather, hydro, inundation, or precip tree | Not a weather research git; throwaway the user kept off the index |
| 2026-09-02 | A two-tone shields badge (`labelColor` plus a second `color`, olive plus maroon here) reads as red and green. Indiana research public markdown (READMEs, lane gists, pointer gist) uses one solid-green `for-the-badge` (`2e7d32`): console/index, lane writeups, standalone parent/sibling repo lines. Table cells and in-sentence `[repo](url)` stay text. Do not empty a lane writeup. | Weather gist/README links on martialsystems Indiana trees | Table GitHub columns; in-sentence citations; non-weather gits |
| 2026-09-02 | Repo-slug shields labels (`indiana_flood_completion`, `indiana__freeze__date` in the URL) are not the catalog names on indiana_wx_pages. Use TREE_TITLE (space `_`, hyphen `--`, underscore `__`, plus `%2B`). Consecutive parent/sibling, lane writeup, and console buttons share one markdown line with a single space. Do not write `Parent:` / `Write-up:` / `Research index:` in front of a button. | Indiana research README/gist green buttons | Table cells; in-sentence `[repo](url)` |
| 2026-08-29 | A new public weather/hydro/precip git ships a question-first README (finding, science SHA, two figures, locked table, footer to gist 66b896b0) and, in the same finish, lands on that readable index gist plus the lane gist. RESEARCH.md in martialsystems/.github is a stub that points at the gist, not a second full copy. Precip is one gist with sections, not a gist per sequel. Bare github.com/martialsystems is not the index. Home rule: ~/.grok/rules/weather-research.md | Creating or first-publishing a martialsystems weather, hydro, inundation, or precip tree | Skip-when: after 2026-09-03 the live console is the index; use the 2026-09-03 row |
| 2026-08-29 | A same-day spatial interpolator (IDW, kriging) is empty if its donors are the ML training rows of a combined temporal+spatial split. Train dates and holdout dates do not overlap. Donors must be train *locations* on the prediction day, including holdout-period days at those sites. Holdout-station labels stay out. | Skill table with a gauge interpolator bar plus a temporal train cut | Split is spatial-only with shared dates; or the interpolator is not same-day |
| 2026-08-29 | RMSE can improve while MAE gets worse. That is peaks, not a closed mechanism. Keep both columns. Do not write the target as explained on RMSE alone. | Multi-feature holdout skill tables | MAE also improves, or the product is RMSE-only by contract |
| 2026-08-29 | A RMSE edge much smaller than the honesty bar already used on a sibling failure (Ridge lost to persistence here) is a weak yes, not a closed mechanism. Keep the table. Do not write the second target as explained. | Multi-target skill on one frozen split after a stricter negative sibling | The second-target margin is comparable to the primary result |
| 2026-08-28 | Two official gages on the same named tributary: a complete daily 00060 on the locked window beats a mouth-ward site that starts after train start. Document the incomplete site. Empty 00060 is fetch-or-stop, not an invented third site. | NWIS (or similar) tributary selection on a frozen train/holdout split | Only one official site exists; or the mouth-ward record covers train |
| 2026-08-28 | USGS waterservices can return HTTP 200 then raise `IncompleteRead` on a chunked body. Retry it. Do not treat the first truncated read as empty 00060. | NWIS DV/IV JSON over `urlopen` | The client already retries `IncompleteRead`; or the feed is a local file |
| 2026-08-29 | A host auto-deploy that runs `vbd_gate` origin-ahead on a tree dirtied by generated board JSON will freeze the public site while logs keep growing. Autostash only generated paths, ff-only pull, re-export from host logs, then gate. Do not skip `vbd_gate`. Do not `reset --hard`. Unexpected dirty + origin ahead still fails closed. | Host or CI Pages publisher that exports JSON into a tracked `public/data` tree | Deploy does not run `vbd_gate`; or generated JSON is gitignored |
| 2026-08-28 | An official point/polygon API that returns HTTP 200 and `[]` is still a missing layer. Fetch-or-stop. Do not paint a zero-point map as a live result. | Live overlay that must come from an official agency feed | Fixture path; or the empty set is the documented scientific result |
| 2026-08-28 | NWIS DV JSON can be HTTP 200 with `timeSeries: []` for a parameter that still exists as IV (03351000 gage height 00065 here). Empty DV is not "no stage." If the contract allows it, take daily max of IV in chunks. Stop only when DV and IV are both empty. | NWIS daily values as labels or features | The DV `timeSeries` is populated; or the product is DV-only by contract |
| 2026-08-28 | A coefficient or importance grid from a model that loses to persistence (or to a two-feature rain-sum baseline) is "where the model looks," not source areas and not a runoff map. Put that sentence next to the grid. A synthetic fixture that beats persistence does not rescue live skill. | Publishing spatial attribution from a regression or GBM | The same holdout shows the model beating persistence, and the caption states that |
| 2026-08-28 | A routed model that beats persistence at an upstream gage and loses downstream, with high bias growing along the river, is a routing / lateral-inflow pattern. Do not summarize it as global model failure. Persistence remains the bar at each site. | Scoring NWM, WRF-Hydro, or similar routed Q vs gages along one stem | A single gage; or the model loses everywhere including the headwater site |
| 2026-08-28 | USGS FIM GIS for pre-2015 pilots often 404s on `pubs.usgs.gov/sir/.../downloads/`. The same library can still live as NWS partner FIM: `https://water.noaa.gov/resources/downloads/fim/<wfo>/<nwsli>/shapefile/`. Confirm the zip metadata cites the SIR before using it. NWM HAND on maps.water.noaa.gov is a different product. | Comparing to an official USGS inundation library | The SIR page already ships a shapefile zip; or the overlay is not USGS FIM |
| 2026-09-02 | An official inundation library on a named river can sit in a neighboring HUC-8 from the research HAND template. Query WBD at the library gages before clipping. Empty overlap is stop, not a substitute product (another SIR, bathymetry, a state portal). | HAND or cheap paint vs an official FIM library | Library gages already share the HAND HUC-8; or the contract is whole-HUC |
| 2026-09-02 | Operational NWM analysis-assim and a retrospective zarr are different products. Score them in separate tables. A later AnA win does not restamp an earlier retro miss, and a retro miss does not decide AnA. Fetch-or-stop per operational day. | Scoring NWM AnA and NWM retrospective on the same gages | Only one archive exists; or the contract is a single product |
| 2026-09-02 | NWM `analysis_assim` `tm00` is a cycling analysis that has seen recent observations. Caption it as analysis. Do not caption it as a forecast, as medium-range, or as "NWM now wins" after a retrospective miss. | Publishing operational NWM AnA skill | The product is explicitly a forecast cycle (short-range, medium-range) |
| 2026-09-02 | If the preferred NWM forecast cycle 404s at the locked lead (short-range `f024` here), take the documented alternate cycle. Do not fall back to `analysis_assim` tm00 and call it a lead. Per-day 404 is a hole: log fetched/attempted, do not impute. | Scoring NWM short-range or medium-range vs persistence | The preferred cycle exists for the whole window |
| 2026-08-28 | A two-sided gage window vs a library that starts at the gage and runs downstream makes IoU look like disagreement even when the library is almost a subset of HAND. Lead with "N of M library-wet cells are also cheap-wet (K miss)". Put IoU in that same paragraph as the superset / domain reason. Do not say the cheap paint covers the library when K > 0. | HAND or other cheap paint vs a hydraulic map library on a clipped window | Both layers share the same modeled domain |
| 2026-08-28 | WSE = gage zero + stage in one vertical datum (NAVD88 here: 710.51 + 11.0 = 721.51 ft). `h_channel` is the DEM at the stream cell, not gage zero. `Δ = WSE - h_channel`. Using gage datum as `h_channel` makes `Δ` equal to stage and paints the wrong height. | Stage-driven HAND or similar bathtub from a gaged WSE | No vertical datum, or the model is explicitly stage-as-depth |
| 2026-08-28 | Put `Δ` and DEM-minus-datum on the wet-mask figure. A 30 m HAND grid cannot place the full published stage on the floodplain; the missing depth is inside the unresolved channel. Without those two numbers, a shallow wet strip is read as under-flooded. | Stage or HAND inundation figures compared to a published flood stage | The model is explicitly stage-as-depth with no DEM offset |
| 2026-08-28 | A second scenario on a frozen HAND window is a new file and a new Δ, not a new model and not an overwrite of the locked figure. Hash the v1 PNG before and after; refuse if it changes. | Adding a second stage, date, or event overlay to an existing inundation window | The first figure is still draft and the operator asked to replace it |
| 2026-08-28 | When two wet masks on the same window differ by N cells, report leftover target-class fill and extra other-class counts (SFHA dry remaining vs unshaded X). IoU alone hides a split. | Comparing two inundation or class masks on one grid | A single mask; no class overlay |
| 2026-08-28 | A named river centerline may be NHD `ftype` 558 Artificial Path (through a waterbody) rather than 460 StreamRiver. Filtering `ftype=460` drops the main stem. Query `gnis_name` (and log ftypes). | NHD flowlines for a named river, HAND, or burn mask | Layers that are already known to be StreamRiver-only |
| 2026-08-28 | A window crop written to its own GeoTIFF must be read as the whole file. Re-applying the parent `Window(col0, row0, …)` to the crop is a shape mismatch or a silent wrong slice. | Reading a raster that was already windowed from a larger template | Always reading the parent dataset |
| 2026-08-28 | `python3 -m pytest` on a stock Mac is Command Line Tools 3.9 with no GDAL/rasterio. Test modules that import those at collection time surface as N ERRORS, not failures. Keep array tests import-light; `importorskip` IO tests; document `.venv/bin/python -m pytest`. | Pytest plus native GIS/scientific wheels | Pure-stdlib tests; CI image already has the venv |
| 2026-08-27 | A public GitHub repo needs a recognized LICENSE file (MIT or Apache-2.0 on the code). "All rights reserved" in the README with no LICENSE, or a LICENSE that is only ARR, reads as unlicensed/other and confuses GitHub. | Publishing or flipping a repo to public | Stays private; LICENSE already MIT or Apache-2.0 |
| 2026-08-27 | If a headline list of named cases is selected by a window-max (or peak) statistic, the matching mean (or footprint) must sit in the same table row or text window. A name-only list is an overclaim even when banned phrases are absent. | Reports that name entities chosen by max-in-window, peak, or envelope stats | The contract uses only the max; or the names never appear |
| 2026-08-27 | A classifier trained on a reweighted or stratified sample can rank well (PR-AUC above the prevalence baseline) while Brier is worse than the constant classifier. Log calibration separately from ranking. Do not treat an uncalibrated 0.75 as a cutoff. | Probability models trained with oversampling, class weights, or 1:k negatives | Rank-only scores never used as probabilities |
| 2026-08-27 | If a metric is defined along a flow path or graph, Euclidean nearest-neighbor is the wrong oracle. Write a ridge case where the nearest object is not the drained one. | HAND, cost-distance, network distance, any along-path height or time | Distances the contract defines as Euclidean |
| 2026-08-27 | A coverage raster is not verified by a non-zero count of a rare class. Sample cells that must be the majority class if the source is whole (downtown unshaded Zone X, not only SFHA). A REST extract or filter that drops the large polygons will still look fine on A/AE counts. | Rasterizing or fetching a classified map (flood zones, soils, land cover) that later filters rows | Unit-only synthetic grids; the layer is a point inventory |
| 2026-08-25 | A host SPA control (chapter label, previous-route heading, player chrome) can keep the previous document's text after navigation. Do not use that string as the new document's identity unless it is in the current document's list and the current document id matches (video-id vs URL). Prefer time or position against the current list. Test leftover chrome from document A on document B through the production reader. | Extensions, scrapers, or injectors that key off third-party SPA chrome | First-party pages; labels the product writes itself |
| 2026-08-25 | Do not define a project by what it is not. Write from the data we have. No `What it is not` heading on product copy, READMEs, or site pages. Legal/T&C and ops gates already in that repo (first Kalshi research watcher: no live orders) stay. Do not copy this law into other product READMEs. Leave existing site pages unless the user asks to rewrite. | Any README, site copy, PDF, or report the assistant writes | User asked what it is not; editing existing legal/T&C |
| 2026-08-25 | A fail-closed banned-token scan of a whole report JSON will match the ban list itself and "this is not X" disclaimers (`casualty estimate` in a `not` array). Write disclaimers without the banned tokens, or scan only designated claim fields. Fixture-run the production report writer, not only a hand-built dirty string. | Claim/ban scanners over generated reports | Scanner that already excludes metadata keys; no generated report |
| 2026-08-25 | Rebuilding a Cloudflare Pages tree by HTTP GET: `/foo/index.html` 404s when only `foo.html` is deployed (pretty URLs, no splat `_redirects`). Requiring those indexes fail-closes the publisher. Do not "fix" that by deploying the incomplete whitelist: wrangler replaces the whole project and wipes routes not in the list. Mirror every HTTP-reachable live file, vendor `_headers`/`_redirects` (Pages does not serve them over HTTP), skip html-only folder indexes, and refuse deploy if a page directory lacks `index.html`. Sitemap locs that 200 fill gaps in the list. | CI or scripts that rebuild Pages from live URLs then `wrangler pages deploy` | Deploy of a complete git `public/` tree (`deploy_viewer.sh`) |
| 2026-08-24 | A control injected into a host SPA must reattach on navigation and DOM rebuild. A one-shot insert at `document_idle` disappears when the host replaces the action row. Prefer the native sibling (like/dislike), keep insert idempotent, listen for the host navigate event, and test the mount picker plus a missing-bar fallback. | Injecting UI into a third-party page that is an SPA | First-party pages you own; static host HTML |
| 2026-08-24 | Concurrent get → mutate → set on shared storage (chrome.storage, a JSON file, a Map you persist) drops all but the last writer. Two overlapping unshifts of different rows leave one. Serialize those writes with a promise chain, and test N>1 overlapping writes through the production function, not a comment that "races are unlikely." | Any ledger, queue, history, or settings read-modify-write | Append-only logs with no merge; single-writer by construction |
| 2026-08-24 | Indirect verification is not verification. Isolated unit tests, JSON-shape checks, and source-list counts do not prove the production path. Name the exact execution tool or script. If the repo has `vbd.runtime.json`, `vbd_gate` runs those argv commands on `--claim-done` and pre-push (not Stop, not pre-commit). Optional `paths` globs skip a command when the change set does not match. For time/state batching, write a regression of N>1 same-timestamp items through the production id/merge/dedupe path before claiming done. | Any finish that claims a pipeline, import, merge, service worker, or other production path is verified | Docs-only; no production runtime path exists (then residual risk) |
| 2026-08-24 | A snapshot copy that enumerates files (not a whole-tree copy) omits new files until the list is updated. Adding `vbd.runtime.json` to this pack did not appear in GraphForge `bundled/verify-before-done` until `VBD_FILES` included that name. | Adding a file to a pack that is copied by a named file list | Whole-tree copies of the repo |
| 2026-08-24 | A batch import that later merges or dedupes on `id` / `stamp_id` is not verified by checking that the source list has N items, or by unit tests with one row. Run N>1 records through the **same id assignment and merge/dedupe** the production path uses. If ids are generated with `Date.now()` in a tight loop, they collide and N becomes 1. | Import, backfill, replay, or migrate of more than one record into history/catalog/storage | Single-record write; no generated id; merge key is not that id |
| 2026-08-24 | Cloudflare Pages: a directory `foo/` without `foo/index.html` wins over `foo.html`, so `/foo` 404s or SPA-falls through to the homepage. Nested `_redirects` `/*.html /:splat` also rewrites `/dir/index.html` to `/dir/index`. Ship `foo/index.html` when a sibling folder exists, and use `/:file.html /:file` for top-level pretty URLs only. Do not treat “the button went to home” as a CSS bug until those two are checked. | Public Pages/static sites with pretty URLs, mixed `foo.html` and `foo/` trees | Single-file sites with no subdirectories; hosts that do not prefer directories |
| 2026-08-24 | A public app folder is not live if only `index.html` is in git. Relative `app.js` / `app.css` / engine files 404 and the page is a dead shell. Commit every file the HTML `src`/`href` names, and sanity-check those relative assets on disk before deploy. | Folder apps (`/concepts/<name>/`) or any HTML with relative script/style URLs | Single self-contained HTML with no local assets |
| 2026-08-23 | `chrome.permissions.request` needs a user gesture. The toolbar popup (or other extension page) must call it on the click/change that enables a site. Relaying to the service worker and requesting there often fails with no prompt. | MV3 optional host or optional permission grant | Permission is already granted, or not using `optional_host_permissions` |
| 2026-08-22 | Chrome's extension popup and options shell already show the extension icon and name. Do not repeat that logo+title in `popup.html` or the user sees a double header. Keep a document `<title>`. | MV3 `action` popup or `options_ui` page | Standalone HTML that Chrome does not wrap |
| 2026-08-22 | An optional control on a measured overlay must be out of flow (`position: absolute`) if `getBoundingClientRect` of the host feeds placement math. Putting it in normal flow changes the box size and then the placement. Keep estimated fallback sizes unchanged. | Overlay or prompt UI whose measured size is used to place it | The control is supposed to grow the host |
| 2026-08-21 | A Grok marketplace plugin that needs a helper must ship that helper in the pinned git tree. A separate Release zip reads as download-and-run to reviewers even when xAI docs allow helper binaries delivered separately. | Submitting or updating a Grok marketplace plugin that runs local code | Helper is an OS tool already on PATH (`git`, `gh`, `python3`) |
| 2026-08-21 | Fetching dated discussion threads (HN Who is hiring, monthly mailing lists) by relevance returns decade-old hits. Sort by date and drop threads older than the current cycle. | Scraping monthly or dated threads | APIs that only return the current cycle |
| 2026-08-21 | A blank location field must not skip a location filter when title or snippet still names a city. Empty location plus Oslo in the comment is not "location unknown, keep it." | Search or scrape with a location include list | No location filter, or records truly have no location text |
| 2026-08-21 | MV3 `script-src 'self'` blocks inline event handlers (`onerror=`, `onclick=`). Chrome logs "Executing inline event handler violates CSP" even when the action is blocked. Do not emit `on*` attributes in extension HTML; bind with `addEventListener`. | MV3 extension UI | Non-extension pages that allow unsafe-inline |
| 2026-08-21 | MV3 extension pages: `<img src>` that 307s across hosts (Cover Art Archive → archive.org → `dn*.archive.org`) often fails, and a generic `/front-250` shortcut 503s while the stored image-id URL 200s. Use the stored URL first, a fallback src list, `host_permissions` for the whole redirect chain, `img-src https: data:` in extension CSP, and a service-worker fetch → data URL if `<img>` still errors. Do not hide an image on `error` if it already decoded (`naturalWidth > 8`). Cache a small thumb locally; full-size is a link, not a second stored file. | MV3 extension UI embedding third-party images via redirecting CDNs | Same-origin, `data:`, or `chrome-extension://` images only |
| 2026-08-20 | Heavy region dividers on a CSS grid belong on the region wrapper, not on each child. Child min-content (pencil notes, chips, badges) will jog a per-cell thick border so the main line looks broken. Keep notes or chips out of flow (`absolute` / `min-w-0` / `overflow-hidden`). | CSS grid with thick region lines and variable child content | No region lines, or children cannot change min-content |
| 2026-08-19 | JS-injected topbar chrome (theme / hamburger) and a first-visit `overflow: hidden` disclaimer are CLS sources. Reserve the chrome slot in first-paint HTML/CSS (`scrollbar-gutter: stable`). Do not `display:none` a panel that fetch later shows: that shifts everything below. | Public HTML with injected chrome or a gate modal | No public HTML / no layout |
| 2026-08-18 | Phone-width (390x844) and desktop (~1280) for UI, layout, nav, or in-page jumps. Named command, not a thought. Skip/hash must land the **visual** target (the chart), not a tall section header. Martialsys boards: `python3 viewer/scripts/viewport_sanity.py`. | Any HTML/CSS/nav/anchor change | No UI (pure backend, data, or docs-only with no layout) |
| 2026-08-21 | `color-scheme: light dark` with hardcoded dark ink (`#111`, `--fg`) on `Canvas` or a transparent body is unreadable in OS dark mode. Put copy on an explicit panel whose background and ink are set, or write a real `prefers-color-scheme: dark` pair. | HTML/CSS that opts into `color-scheme` or uses `Canvas`/`CanvasText` | Forced single theme with explicit fg and bg |
| 2026-08-16 | Fetch before edit. Dirty+ahead: stash, commit, or report both. Finish-later if local is edited and origin is not ahead. Push or say why. | Any git working tree | Not a git repo |
| 2026-08-16 | Do not overwrite a newer deploy artifact with an older one. Missing source keeps dest. | Site, Pages, board JSON, wrangler | No public/deploy copy |
| 2026-08-19 | An eval table that drops unlabeled rows looks frozen even when newer forecasts exist. Keep pending origin dates (actual empty) so last pred date is visible. | Public/lab error tables from forecast vs actual | Table is labeled-only and the copy says so |
| 2026-08-19 | `vbd_gate` is automatic: Grok Stop hook when the agent finishes a turn; git pre-push if the chat never claimed done. User does not run it by hand. | Any finish / push / deploy | Not a git repo and no tracked UI/docs change |
| 2026-08-19 | Hetzner is the martialsys Pages publisher. Host `deploy_viewer.sh` must find `vbd_gate.py` (clone `agent_laws_verify_before_done` at `$HOME` or `/root`, or set `VBD_GATE`). Missing pack is a hard fail, not warn-and-continue. Pre-push belongs on every product git, not only the research viewer. | Host or CI Pages publish; new product clone | Local preview that already has the pack |
| 2026-08-19 | `vbd_gate` appends one JSON line per invocation to `~/.grok/logs/vbd_gate.jsonl` (`VBD_GATE_LOG` override). Tail that file for step evidence. It is not GraphForge audit and not an interaction-map transcript. | After Stop, pre-push, deploy, or `vbd_gate check` | None |
| 2026-08-19 | After a passing VBD-pack push on this PC, publish the runner into private GraphForge (`vbd_gate.py publish-gf`). Enterprise gets new gate versions by GF clone/pull. Public MIT prose can stay. `VBD_SKIP_GF_PUBLISH=1` skips. | Push of `agent_laws_verify_before_done` | No GraphForge checkout; or skip env set |
| 2026-08-19 | Skip-landing evaluates every `.skip-juicy` hash href, not only the first. A first skip to the chart wrap must not hide a later skip onto the panel. | HTML with skip/hash jumps | No skip controls |
| 2026-08-19 | When adding a field to derived summaries, rebuild from the source artifacts and **lock** the fields that must not change (MAE/RMSE/n here) before any write. Fail the whole batch on drift. Do not retrain to attach a new metric. | Derived JSON/CSV rebuilt from prediction or eval logs | Brand-new artifact with no prior numbers |
| 2026-08-19 | A pipeline-generated markdown file is not a durable addendum store. Re-insert later records from a machine JSON, or keep them in a file the generator does not overwrite. Hand-editing `FREEZE_DECISION.md` (or similar) is lost on the next default run. | Pipeline writes a human report that later research appends to | Hand-authored docs only |
| 2026-08-19 | Before a new freeze recipe overwrites the locked eval JSON, snapshot the previous summary under a named baseline path. The lift must remain auditable after the default walk-forward is the new recipe. | Replacing a locked model recipe or freeze file | First freeze with no prior locked numbers |
| 2026-08-20 | A 10-bin PSI on ~21 daily rows (one calendar month) is degenerate and will CRITICAL on ordinary regime noise. Use a longer recent vs reference window (63 vs 252 here). Do not hard-block promote on raw non-stationary **levels** (range, RV) when PSI is a corruption gate; score those as report-only and block on scores / stationary transforms. | PSI as a promotion or freeze gate | PSI is only a diagnostic table with no block |
| 2026-08-20 | A paper/hold-out scorer that shares a production job's close must be a **separate workflow**, never `&&` inside the live pipeline. Fail isolation: the live job must succeed even if the sibling scorer dies. The paper job must not set live flags or write the freeze file. | Adding a second model on the same daily close as a live pipeline | The second path is already a separate repo/job |
| 2026-08-20 | Email/Slack alerts on a live pipeline must not change the job conclusion. Missing SMTP secret or a send error is a warning or fallback, never a red forecast. Success with no drift stays silent. | Adding mail/webhook alerts to a live CI job | The job's only purpose is to send the alert |
| 2026-08-20 | Fail-closed raw-bar checks belong **before** a live predict append, not only in a post-hoc validate step that can be skipped. Calendar vs the exchange, zero volume, and adjustment jumps are the usual silent-corrupt paths. | Live forecast or bar-data pipeline that appends a ledger row | Research notebooks that do not write a live log |
| 2026-08-19 | `get_command_or_subagent_output` with a positive timeout and multiple ids is wait-all. Duration is max(children), not tool overhead (successful joins return in ~1s). Use `timeout_ms: 0` snapshots or `wait_any`. Pipeline the next phase when the first child finishes. | Parallel Grok subagents | One child, or no subagents |
| 2026-08-19 | A new versioned manifest added on an old base stays stale after rebase onto a bumped tree. Set the new file's version to current `package.json` (or equivalent) and register it in the bump list in the same change. | Adding a plugin/extension manifest + version-bump registry | Single-file version, or no registry |
| 2026-08-20 | Do not glob-replace em/en dashes with colons or hyphens. Classify the dash (label, aside, contrast, range, cut-off) and rewrite so the sentence still parses. Colon is not a universal substitute. | Any prose/docs edit that removes dashes | User asked to keep decorative dashes |
| 2026-08-21 | A lockfile generated on a newer CPython than CI will fail `pip install` (numpy 2.5 / scipy 1.18 need >=3.12; 3.11 has no wheels). Pin Actions `python-version` to the lock header, and keep a test that the declared minor satisfies Requires-Python for the scientific pins. Failure-path alerts must not import lock-only deps (lazy-import `requests` for GitHub fallback; SMTP is stdlib). | `requirements.lock` (or equivalent) + CI `python-version` | No lockfile, or CI uses the same venv that wrote the lock |
| 2026-08-21 | A vendor daily bar printed during RTH is not a completed session. Trim bars after `expected_last_session` at ingest. Do not fail the whole live job while the last completed session is present (that blocks catch-up). Fail closed only when the completed session is missing or stale. | Live daily forecast ingest from Yahoo-like vendors | Vendor returns only completed sessions, or the job is intraday-native |

Project-only scripts stay in that repo. Promote the **check**, not a one-off path, unless the same surface exists elsewhere.
